# Data Model: Coolify Docker Deploy

**Not applicable.**

This feature ships infrastructure files (`Dockerfile`, `nginx.conf`, `.dockerignore`) only. It introduces no domain entities, no database tables, no persisted state, and no in-memory data structures.

The communicAid SPA's existing data model (categories, items, recent items, quick names) is unchanged by this feature.
